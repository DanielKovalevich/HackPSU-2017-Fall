var express = require('express');
var router = express.Router();

/* GET test function. */
router.get('/fundamental', function (req, res, next) {

    //Add some sort of response code here.
    //Did the SQL table encounter any errors? Did it update properly? Alert the user if it went bad

});

module.exports = router;